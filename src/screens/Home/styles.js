import {Fonts, Colors} from '../../theme';

const styles = {
  root: ({themeId}) => ({
    // backgroundColor: Colors[themeId].secondary,
    // display: "flex",
    flex: 1,
  }),
};

export default styles;
